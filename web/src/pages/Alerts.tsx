import { BellRing, Plus, SlidersHorizontal } from "lucide-react";
import { EmptyState } from "../components/State";
export function Alerts({ available }: { available: boolean }) { return <section className="page-section"><div className="section-head"><div><p className="eyebrow">NOTIFICATION RULES</p><h2>Alerts</h2></div><button className="outline-btn"><Plus size={15}/> New alert</button></div><div className="alert-setup"><BellRing size={24}/><div><h3>Turn market movement into a signal</h3><p>Define a gift or edge threshold and get notified when live data confirms it.</p></div><SlidersHorizontal size={20}/></div>{available ? <div className="alert-row"><span className="status-toggle"/><div><strong>Opportunities above 10%</strong><small>All verified collections · active</small></div><button className="outline-btn">Edit</button></div> : <EmptyState title="No alerts configured" detail="Create a rule after your first live market snapshot."/>}</section>; }
